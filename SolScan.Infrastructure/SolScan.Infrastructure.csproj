<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <PlatformTarget>x64</PlatformTarget>
    <Platforms>AnyCPU;x64</Platforms>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\SolScan.Core\SolScan.Core.csproj" />
  </ItemGroup>

  <!-- Native camera SDK binaries (e.g. ZWO ASICamera2.dll, x64) will ship alongside this
       assembly once the camera wrapper lands - see SolScan CLAUDE.md Phase 4. -->

</Project>
