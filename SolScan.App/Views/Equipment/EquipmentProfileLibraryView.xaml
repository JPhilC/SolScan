<UserControl x:Class="SolScan.App.Views.Equipment.EquipmentProfileLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Library list -->
        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
                <Button Content="Add" Width="70" Margin="0,0,8,0" Command="{Binding AddCommand}"/>
                <Button Content="Remove" Width="70" Command="{Binding RemoveCommand}"/>
            </StackPanel>
            <ListBox ItemsSource="{Binding Items}"
                     SelectedItem="{Binding SelectedItem}"
                     DisplayMemberPath="Label"/>
        </DockPanel>

        <!-- Selected telescope/camera profile's fields - blank/inert until something is selected -->
        <Grid Grid.Column="2" DataContext="{Binding SelectedItem}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="240"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Label"/>
            <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Telescope"/>
            <TextBox Grid.Row="2" Grid.Column="2" Text="{Binding Telescope, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Telescope focal length (mm)"/>
            <TextBox Grid.Row="4" Grid.Column="2" Text="{Binding TelescopeFocalLengthMm, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Aperture (mm)"/>
            <TextBox Grid.Row="6" Grid.Column="2" Text="{Binding ApertureMm, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="8" Grid.Column="0" Text="Camera"/>
            <TextBox Grid.Row="8" Grid.Column="2" Text="{Binding Camera, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="10" Grid.Column="0" Text="Camera pixel size (µm)"/>
            <TextBox Grid.Row="10" Grid.Column="2" Text="{Binding CameraPixelSizeMicrons, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="12" Grid.Column="0" Text="Mount"/>
            <TextBox Grid.Row="12" Grid.Column="2" Text="{Binding Mount, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="14" Grid.Column="0" Text="Site latitude (°)"/>
            <TextBox Grid.Row="14" Grid.Column="2" Text="{Binding SiteLatitudeDeg, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="16" Grid.Column="0" Text="Site longitude (°)"/>
            <TextBox Grid.Row="16" Grid.Column="2" Text="{Binding SiteLongitudeDeg, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
    </Grid>
</UserControl>
