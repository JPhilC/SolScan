<UserControl x:Class="SolScan.App.Views.Equipment.TelescopeLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Library list -->
        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
                <Button Content="Add" Width="70" Margin="0,0,8,0" Command="{Binding AddCommand}"/>
                <Button Content="Remove" Width="70" Command="{Binding RemoveCommand}"/>
            </StackPanel>
            <ListBox ItemsSource="{Binding Items}"
                     SelectedItem="{Binding SelectedItem}"
                     DisplayMemberPath="Label"/>
        </DockPanel>

        <!-- Selected telescope's fields - blank/inert until something is selected -->
        <Grid Grid.Column="2" DataContext="{Binding SelectedItem}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="240"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Label"/>
            <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Focal length (mm)"/>
            <TextBox Grid.Row="2" Grid.Column="2" Text="{Binding FocalLengthMm, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:F1}}"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Aperture (mm)"/>
            <TextBox Grid.Row="4" Grid.Column="2" Text="{Binding ApertureMm, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:F1}}"/>

            <TextBlock Grid.Row="6" Grid.Column="0" Text="ERF (Energy Rejection Filter)"/>
            <TextBox Grid.Row="6" Grid.Column="2" Text="{Binding EnergyRejectionFilter, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
    </Grid>
</UserControl>
